{
  "name": "beginner-coding-problems",
  "version": "1.0.0",
  "description": "beginner-coding-problems repository",
  "main": "build/src/index.js",
  "types": "build/src/index.d.ts",
  "files": [
    "build/src"
  ],
  "license": "Apache-2.0",
  "keywords": [],
  "scripts": {
    "build": "yarn run compile",
    "test": "jest --coverage",
    "compile": "tsc -p .",
    "pretty": "prettier --write --tab-width 2 src/**/*.{ts,tsx}"
  },
  "devDependencies": {
    "gts": "^0.9.0",
    "typescript": "^3.4.2"
  },
  "dependencies": {
    "@types/jest": "^24.0.11",
    "@types/node": "^11.13.4",
    "jest": "^24.7.1",
    "prettier": "1.16.4",
    "ts-jest": "^24.0.2"
  }
}
